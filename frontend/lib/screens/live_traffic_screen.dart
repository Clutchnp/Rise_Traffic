import 'package:flutter/material.dart';

class LiveTrafficScreen extends StatelessWidget{
  const LiveTrafficScreen({super.key});

  @override
    Widget build(BuildContext context) {
      return Scaffold();
    }

}
