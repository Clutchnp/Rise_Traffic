import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Base
  static const background = Color(0xFF0B0F14);
  static const sidebar = Color(0xFF11161D);
  static const surface = Color(0xFF151C24);
  static const surfaceElevated = Color(0xFF1B232D);

  // Borders
  static const border = Color(0x0FFFFFFF);

  // Text
  static const textPrimary = Color(0xFFE8EDF2);
  static const textSecondary = Color(0xFF9AA4AF);
  static const textMuted = Color(0xFF626D78);

  // Traffic status
  static const trafficNormal = Color(0xFF39D98A);
  static const trafficModerate = Color(0xFFF5C451);
  static const trafficHigh = Color(0xFFFF9F43);
  static const trafficCritical = Color(0xFFFF5C5C);

  // System
  static const systemOnline = Color(0xFF39D98A);
  static const systemWarning = Color(0xFFF5C451);
  static const systemOffline = Color(0xFFFF5C5C);

  // Accent
  static const accent = Color(0xFF5B8DEF);
}
